<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Arka plan -->
    <solid android:color="@android:color/white" />

    <!-- Kenarlık -->
    <stroke
        android:width="1dp"
        android:color="#D0D0D0" />

    <!-- Boyut (opsiyonel, TextView zaten ölçüyor) -->
    <size
        android:width="56dp"
        android:height="56dp" />
</shape>
